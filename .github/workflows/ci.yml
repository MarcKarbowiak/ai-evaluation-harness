name: CI

on:
  push:
  pull_request:

jobs:
  test-and-mock-gate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest

      - name: Unit tests
        run: pytest -q

      - name: Quality gate (offline mock)
        run: |
          eval-harness run \
            --dataset datasets/sample_tasks.jsonl \
            --prompt prompts/task_extraction/v1.md \
            --schema schemas/task_extraction.schema.json \
            --adapter mock \
            --min-schema-valid-rate 1.0 \
            --min-avg-f1 0.8

      - name: Upload reports artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: reports

  azure-real-gate:
    runs-on: ubuntu-latest
    needs: test-and-mock-gate

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest

      # This step only runs if the secret exists
      - name: Quality gate (Azure OpenAI / Foundry)
        if: ${{ env.AZURE_OPENAI_API_KEY != '' }}
        env:
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          AZURE_OPENAI_BASE_URL: ${{ secrets.AZURE_OPENAI_BASE_URL }}
          AZURE_OPENAI_MODEL: ${{ secrets.AZURE_OPENAI_MODEL }}
        run: |
          eval-harness run \
            --dataset datasets/sample_tasks.jsonl \
            --prompt prompts/task_extraction/v1.md \
            --schema schemas/task_extraction.schema.json \
            --adapter azure \
            --min-schema-valid-rate 1.0 \
            --min-avg-f1 0.8

      - name: Upload reports artifact (azure run)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-azure
          path: reports
